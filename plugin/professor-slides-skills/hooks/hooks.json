{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"process.exit(process.versions.node.split('.').map(Number)[0]>=22?0:1)\" 2>/dev/null || echo 'professor-slides-skills is enabled but no Node 22.6+ is on PATH, so every pres command will fail. Install Node 22.6 or later (it runs TypeScript directly via --experimental-strip-types).'"
          }
        ]
      }
    ]
  }
}
